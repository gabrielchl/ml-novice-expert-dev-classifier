This directory contains earlier Python files used in experiments to train a
model, along with all datasets generated with the original Java program in csv
formats used by those Python training scripts.
